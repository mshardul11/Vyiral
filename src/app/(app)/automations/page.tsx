import { AutomationsClient } from "@/components/automations/automations-client";

export const metadata = { title: "Automations — Vyiral" };

export default function AutomationsPage() {
  return <AutomationsClient />;
}
