import { CompetitorIntelClient } from "@/components/competitors/competitor-intel-client";

export const metadata = { title: "Competitors" };

export default function CompetitorsPage() {
  return <CompetitorIntelClient />;
}
