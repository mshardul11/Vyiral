import { WorkspaceClient } from "@/components/workspace/workspace-client";

export const metadata = { title: "Workspace — Vyiral" };

export default function WorkspacePage() {
  return <WorkspaceClient />;
}
