import { AuditEngineClient } from "@/components/audit/audit-engine-client";

export const metadata = { title: "Channel Audit" };

export default function AuditPage() {
  return <AuditEngineClient />;
}
